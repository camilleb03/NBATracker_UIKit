platform :ios, '9.0'

target 'NBATracker' do
  use_frameworks!

  # Pods for NBATracker
  pod 'SVGKit', :git => 'https://github.com/SVGKit/SVGKit.git', :branch => '3.x'
  target 'NBATrackerTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
